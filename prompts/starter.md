A workshop attendee said they would point an AI agent at this:

"{{ idea_text }}"

Write a 2 to 4 sentence starter prompt they could paste into Claude Code, OpenCode, or OpenClaw to actually try it. Be concrete. Suggest one specific tool or integration where relevant (e.g. an MCP server, a CLI, a Python script). Address the reader in second person.

Respond with ONLY the prompt, no preamble.
